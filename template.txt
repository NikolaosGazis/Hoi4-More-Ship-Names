##### <Country> Ship Names #####

### Prefixes ###
# <prefix> = <prefix's meaning>

### Aircaft Carrier Names ###
<tag>_CV_HISTORICAL = { # Can remove "Historical" if they aren't historical names, just a cosmetic.
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "<fallback_name> %d"

	unique = { # names go here.

	}
}

### Light Aircraft Carrier Names ###
<tag>_CVL_HISTORICAL = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "<fallback_name> %d"

	unique = { # names go here.

	}
}

### Battleship Names ###
<tag>_BB_HISTORICAL = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "<fallback_name> %d"

	unique = { # names go here.

	}
}

### Battlecruiser Names ###
<tag>_BC_HISTORICAL = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "<fallback_name> %d"

	unique = { # names go here.

	}
}

### Heavy Cruiser Names ####
<tag>_CA_HISTORICAL = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "<fallback_name> %d"

	unique = { # names go here.

	}
}

### Light Cruiser Names ###
<tag>_CL_HISTORICAL = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "<fallback_name> %d"

	unique = { # names go here.

	}
}

### Destroyer Names ###
<tag>_DD_HISTORICAL = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "<fallback_name> %d"

	unique = { # names go here.

	}
}

### Submarine Names ###
<tag>_SS_HISTORICAL = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "<fallback_name> %d"

	unique = { # names go here.

	}
}

##### Themes #####

### Theme: Regions ###
<tag>_THEME_REGIONS = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "Ship-A %d"

	unique = { # names go here.

	}
}

### Theme: Mountains ###
<tag>_THEME_MOUNTAINS = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "Ship-B %d"

	unique = { # names go here.

	}
}

### Theme: Cities ###
<tag>_THEME_CITIES = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "Ship-C %d"

	unique = { # names go here.

	}
}

### Theme: Rivers ###
<tag>_THEME_RIVERS = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "Ship-D %d"

	unique = { # names go here.

	}
}

### Theme: Famous Battles ###
<tag>_THEME_BATTLES = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "Ship-E %d"

	unique = { # names go here.

	}
}

### Theme: Mythology ###
<tag>_THEME_MYTHOLOGY = {
	name = <name>

	for_countries = { <tag(s)> }

	can_use = {  } # if it needs a specified flag because formable or for a specific ideology.

	type = ship # don't change
	ship_types = { <hulls> }

	prefix = "<prefix> " # leave a space.
	fallback_name = "Ship-F %d"

	unique = { # names go here.

	}
}
